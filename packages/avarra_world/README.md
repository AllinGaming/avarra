# avarra_world

Server-safe loading and validation for portable AVARRA world definitions.

Stage 4 uses a single JSON `.avarra` document as a prototype container. That
choice is intentionally provisional; the versioned world model, stable IDs,
component schemas, reference validation, and ECS-loading boundary are the
durable contracts established here.
